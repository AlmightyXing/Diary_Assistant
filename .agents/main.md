# Agent Skills 配置

## Agent skills

### Issue tracker

GitHub issues (no PR triage). See `rules/issue-tracker.md`.

### Triage labels

Standard vocabulary mapping. See `rules/triage-labels.md`.

### Domain docs

Single-context layout (root `CONTEXT.md`). See `rules/domain.md`.
