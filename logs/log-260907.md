# 日志 2026-09-07

- [20:17] 完成优化项一：日程弹窗增加“日程日期”选项。修改了 `App.tsx` 中的 `scheduleForm` 状态加入 date 字段，默认选取当日。在保存时，如果是新建到其他日期，也会自动切换界面的日期视图，方便连贯查看。
- [20:41] 清理了上一项中的“自动跳转日期视图”功能。开始执行优化项二：修复应用聚焦记录每日不自动清空的 Bug。定位发现该情况发生在应用重启跨天时。通过在后端 SQLite 的 settings 表中新增 `last_stats_date` 持久化记录，使得无论是在运行中跨天，还是在第二天重新启动软件，系统都能正确识别出日期变更并清空 `app_stats` 表的数据。
- [20:51] 插入新需求实现：重构“健康提醒”效果。通过 Electron 的 IPC 唤起全屏无边框透明窗口（flashWin）来实现真正的“主屏幕级边框呼吸灯闪烁”效果，并使用 Web Audio API 编写了“滴-滴-滴”的警报音效替代原本无效的空音频。
- [22:30] 执行健康监控底层重构（方案二）：将健康监控与弹窗逻辑移至 Electron Main Process 处理，彻底避免前台节流导致的卡死与重复触发。在 Python 后端 `HealthTracker` 增加了系统休眠检测逻辑，若电脑休眠超过5分钟，再次唤醒时自动回满倒计时。
- [23:18] 修复应用白名单管理：将前端拉取的 API 从 `/running-apps` 指向了后端的 `/installed-apps`。现在应用下拉列表将通过扫描系统的“开始菜单”快捷方式来生成，只展示用户常规安装的“应用”（例如 Chrome、WeChat），彻底屏蔽了杂乱无章的后台系统进程，并且无论应用当前是否在运行都可以直接添加到白名单中。
- [23:26] 排查问题四（模型API无法运行）：确认该问题属于旧版打包环境缺失 SSL/证书链导致。目前代码中已加入了 `ssl._create_unverified_context()` 补丁绕过了证书验证，且开发环境下通过 `localStorage` 正常下发 API Key。该问题确认为已解决，正式划掉。
- [23:43] 第五项问题修复（小组件与日历彻底解耦）：重构了应用前端和主进程的 IPC 交互逻辑。现在`日程日历`与`健康助手`将独立控制它们对应的 `CalendarWidget` 和 `Widget` 的启动与关闭。移除了 `Widget.tsx` 中遗留的冗余日程代码。经核查，二者独立窗口的位置记忆（`moved`事件回调）运转正常。
